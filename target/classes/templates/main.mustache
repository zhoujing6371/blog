<html>
<!--Title-->
<body style="background-color:aliceblue;"/>
<nav class="navbar navbar-expand-lg navbar-light bg-info">
    <a class="navbar-brand" href="/">Blog</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
</nav>
<!---->
<!--text style-->
<meta charset="UTF-8">
<title>Blog</title>

<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css"
      integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous">
<!---->
<title>My Blog</title>
<body>
<div class="container">
    <div>

        <form method="post">
            <p></p>
            <input type="text" required name="text" placeholder="Enter your message:"/>
            <input type="text" name="tag" required placeholder="Enter your TAG:">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
            <button type="submit">Publish</button>
        </form>
    </div>
    <div>
        <form action="/logout" method="post">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
            <input type="submit" value="Sign Out"/>
        </form>
    </div>

    <div>
        You can delete message that you don't like (by message ID):
        <form method="post" action="delete">>
            <input type="number" name="deleteById" min="1" required placeholder="Delete message by ID"/>
            <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
            <button type="submit">Delete</button>
        </form>
    </div>

    <div>
        You can find message (by message ID) and update text. It need to be your message!
        <form method="post" action="findById">
            <input type="text" name="findById" required placeholder="Find/Update message by ID">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
            <button type="submit">Find</button>
        </form>
    </div>

    <div>
        You can find message (by message TAG):
        <form method="post" action="filter">
            <input type="text" name="filter" required placeholder="Find bu TAG:">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
            <button type="submit">Find</button>
        </form>
    </div>


    {{#messages}}
        <div>
        User ID: <span>{{id}}</span>
        User Name:<strong> ({{authorName}})</strong>;
        Tag:<i>({{tag}})</i> <br>
        Blog Context:<b>{{text}}</b> <br><br><br>        </div>
    {{/messages}}
</div>
</body>
</html>